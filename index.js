/**
 * @format
 */

import {AppRegistry} from 'react-native';
import SingtelApplication from './app/SingtelApplication';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => SingtelApplication);
